export enum NotificationTypes {
	PLATFORM_UPDATE = "Platform update",
	COMMENT_TAG = "Comments tag",
	ACCESS_GRANTED = "Access granted",
	JOIN_WORKSPACE = "Join workspaces",
};

export enum ToastTypes {
	SUCCESS = "success",
	ERROR = "error",
	INFO = "info",
};